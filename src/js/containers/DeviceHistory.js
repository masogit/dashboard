import React, { Component } from 'react';
import { Box } from 'grommet';

export default class DeviceHistory extends Component {
  render() {
    return (
        <Box>
            This is Device History.
        </Box>
    );
  }
}
